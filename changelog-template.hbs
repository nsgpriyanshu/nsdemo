# All notable changes

## All the notable changes are mentioned below

{{#each releases}}
  ---
  ## [{{this.version}}] - {{datetime this.date 'YYYY-MM-DD'}}

  {{#if this.notes}}
    {{#each this.notes}}
      {{#if this.commits}}
        {{#each this.commits}}
          {{#if this.scope}}
            ### {{capitalize this.scope}}
            - {{this.subject}}
          {{else}}
            ### {{capitalize this.type}}
            - {{this.subject}}
          {{/if}}
        {{/each}}
      {{/if}}
    {{/each}}
  {{/if}}
{{/each}}